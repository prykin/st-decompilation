
void FUN_0055db70(void)

{
  /* ST_PSEUDO[unresolved_register_input]: candidate live-in register: verify boundary, SEH/setjmp ABI, or convention */
  cMf32 *in_stack_00000004;

  if (DAT_008032a4 != 0) {
    /* ST_PSEUDO[unresolved_register_input]: candidate live-in register: verify boundary, SEH/setjmp ABI, or convention */
    cMf32::RecMemFree(in_stack_00000004,(uint *)&DAT_008032a4);
  }
  if (DAT_008032a8 != 0) {
    /* ST_PSEUDO[unresolved_register_input]: candidate live-in register: verify boundary, SEH/setjmp ABI, or convention */
    cMf32::RecMemFree(in_stack_00000004,(uint *)&DAT_008032a8);
  }
  if (DAT_008032ac != 0) {
    /* ST_PSEUDO[unresolved_register_input]: candidate live-in register: verify boundary, SEH/setjmp ABI, or convention */
    cMf32::RecMemFree(in_stack_00000004,(uint *)&DAT_008032ac);
  }
  if (DAT_008032b0 != 0) {
    /* ST_PSEUDO[unresolved_register_input]: candidate live-in register: verify boundary, SEH/setjmp ABI, or convention */
    cMf32::RecMemFree(in_stack_00000004,(uint *)&DAT_008032b0);
  }
  return;
}

