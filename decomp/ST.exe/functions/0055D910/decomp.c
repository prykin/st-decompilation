
/* [STReturnSemanticsApplier] ignored_eax_void.
   Evidence: all observed direct callers ignore the return register (ignored=3, used=0), and
   decompilation contains no value return */

void FUN_0055d910(void)

{
  /* ST_PSEUDO[unresolved_register_input]: candidate live-in register: verify boundary, SEH/setjmp ABI, or convention */
  cMf32 *in_stack_00000004;

  if (PTR_008032b4 != nullptr) {
    /* ST_PSEUDO[unresolved_register_input]: candidate live-in register: verify boundary, SEH/setjmp ABI, or convention */
    cMf32::RecMemFree(in_stack_00000004,(uint *)&PTR_008032b4);
  }
  if (PTR_008032b8 != nullptr) {
    /* ST_PSEUDO[unresolved_register_input]: candidate live-in register: verify boundary, SEH/setjmp ABI, or convention */
    cMf32::RecMemFree(in_stack_00000004,(uint *)&PTR_008032b8);
  }
  if (PTR_008032bc != nullptr) {
    /* ST_PSEUDO[unresolved_register_input]: candidate live-in register: verify boundary, SEH/setjmp ABI, or convention */
    cMf32::RecMemFree(in_stack_00000004,(uint *)&PTR_008032bc);
  }
  if (PTR_008032c0 != nullptr) {
    /* ST_PSEUDO[unresolved_register_input]: candidate live-in register: verify boundary, SEH/setjmp ABI, or convention */
    cMf32::RecMemFree(in_stack_00000004,(uint *)&PTR_008032c0);
  }
  if (PTR_008032c4 != nullptr) {
    /* ST_PSEUDO[unresolved_register_input]: candidate live-in register: verify boundary, SEH/setjmp ABI, or convention */
    cMf32::RecMemFree(in_stack_00000004,(uint *)&PTR_008032c4);
  }
  if (PTR_008032c8 != nullptr) {
    /* ST_PSEUDO[unresolved_register_input]: candidate live-in register: verify boundary, SEH/setjmp ABI, or convention */
    cMf32::RecMemFree(in_stack_00000004,(uint *)&PTR_008032c8);
  }
  if (PTR_008032cc != nullptr) {
    /* ST_PSEUDO[unresolved_register_input]: candidate live-in register: verify boundary, SEH/setjmp ABI, or convention */
    cMf32::RecMemFree(in_stack_00000004,(uint *)&PTR_008032cc);
  }
  if (PTR_008032d0 != nullptr) {
    /* ST_PSEUDO[unresolved_register_input]: candidate live-in register: verify boundary, SEH/setjmp ABI, or convention */
    cMf32::RecMemFree(in_stack_00000004,(uint *)&PTR_008032d0);
  }
  if (PTR_008032d4 != nullptr) {
    /* ST_PSEUDO[unresolved_register_input]: candidate live-in register: verify boundary, SEH/setjmp ABI, or convention */
    cMf32::RecMemFree(in_stack_00000004,(uint *)&PTR_008032d4);
  }
  PTR_008073cc = PTR_008032c0;
  DAT_00807560 = PTR_008032c4;
  DAT_008073c8 = PTR_008032bc;
  return;
}

